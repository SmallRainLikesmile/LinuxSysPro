#include <sys/stat.h>
#include <fcntl.h>
#include "tlpi_hdr.h"

int mydup(int oldfd);

int main(int argc, char * argv[])
{
	int iFile, iFileFlags;
	mode_t mtFileMode;



	exit(EXIT_SUCCESS);
}
int mydup(int oldfd)
{
	
}
